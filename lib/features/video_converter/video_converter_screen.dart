import 'package:flutter/material.dart';

class VideoConverterScreen extends StatelessWidget {
  const VideoConverterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Video Converter')),
      body: const Center(child: Text('Video Converter - Coming Soon')),
    );
  }
}