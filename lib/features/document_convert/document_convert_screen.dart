import 'package:flutter/material.dart';

class DocumentConvertScreen extends StatelessWidget {
  const DocumentConvertScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Document Convert')),
      body: const Center(child: Text('Document Convert - Coming Soon')),
    );
  }
}